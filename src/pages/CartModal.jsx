import React from "react";
import { useModal } from "../context/ModalContext";

const CartModal = () => {
  const { state, modal, addCartModal, addFavoriteModal } = useModal();
  return <></>;
};
export default CartModal;
